<div class="row">
% for my $a (@{$achievement->to_array}) {
  <div class="col-sm-4">
  <blockquote class="<%= $a->{new} ? 'bg-info' : '' %>">
    <%= $a->{data} %><footer><%= $a->{time} %></footer>
  </blockquote>
  </div>
% }
</div>
