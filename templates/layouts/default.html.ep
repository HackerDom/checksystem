<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="initial-scale=1" />
    <link rel="stylesheet" href="/css/bootstrap.css">
    <script src="/mojo/jquery/jquery.js"></script>
    <title><%= title %></title>
    <style type="text/css">
html { position: relative; min-height: 100%; }
body { margin-bottom: 165px; }
.footer {
  padding-top: 5px;
  margin-top: 5px;
  overflow: scroll;
  overflow-x: hidden;
  position: fixed;
  width: 100%;
  bottom: 0;
  height: 165px;
  background-color: #f5f5f5;
}
.table-condensed>tbody>tr>td { padding: 4px; }
    </style>
  </head>
  <body>
    <div class="navbar navbar-default navbar-static-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <a class="navbar-brand" href="<%= url_for 'index' %>"><%= app->config->{cs}{ctf}{name} %></a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><div id="round" class="navbar-text"><%= content_for 'r' %></div></li>
          </ul>
        </div>
      </div>
    </div>
    <div class="container">
      <%= content_for 'progress' %>
      <table class="table table-condensed">
        <tr class="text-center">
          % for my $status (@{app->model('checker')->statuses}) {
            <td class="col-md-3" bgcolor="#<%= $status->[1] %>"><%= $status->[0] %></td>
          % }
        </tr>
      </table>
      <%= content %>
    </div>
    % if (current_route =~ /index/) {
      <footer class="footer">
        <div class="container"><%= content_for 'footer' %></div>
      </footer>
    % }
    <script>
      $('img').error(function() { $(this).attr('src', '/logo.png'); });
    </script>
  </body>
</html>
